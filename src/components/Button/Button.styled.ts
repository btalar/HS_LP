import { Button } from "@nextui-org/react";
import styled from "styled-components";

export const BtnWrap = styled(Button)`
  border: 1px solid var(--primary-5, #A36A2F);
  background: var(--primary-1, #F5EEE8);
  font-size: 20px;
`;